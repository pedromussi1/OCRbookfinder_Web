# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Local Google Books response cache (regenerated by eval.py)
data/cache/

# User-uploaded images at runtime (keep the labeled dataset in static/uploads that
# is referenced by data/labels.csv; ignore ad-hoc uploads if you separate them later)
# Note: the 5 labeled seed images ARE committed; runtime uploads can be cleaned manually.

# OS / editor
.DS_Store
.idea/
.vscode/

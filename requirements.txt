# Core dependencies (baseline + preprocess + fuzzy rungs, and the web app).
# The semantic/hybrid rungs need the heavier extras in requirements-semantic.txt.
Flask==3.1.3
Werkzeug==3.1.8
opencv-python==5.0.0.93
pytesseract==0.3.13
numpy==2.4.6
requests==2.34.2
RapidFuzz==3.14.5
pytest==9.1.1
gunicorn==23.0.0  # production WSGI server (Linux/Docker); use waitress on Windows
